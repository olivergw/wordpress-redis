name: Build and Push to Docker Hub

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

env:
  DOCKER_IMAGE: olivergw/wordpress-redis

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      
      - name: Log in to Docker Hub
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract Redis image tags
        id: redis_version
        run: |
          REDIS_TAG=$(awk '$1 == "FROM" && $2 ~ /^redis:/ { sub(/^redis:/, "", $2); print $2; exit }' Dockerfile)
          REDIS_VERSION="${REDIS_TAG%%-*}"
          REDIS_VARIANT="${REDIS_TAG#*-}"
          echo "tag=${REDIS_TAG}" >> "$GITHUB_OUTPUT"
          echo "minor=${REDIS_VERSION%.*}" >> "$GITHUB_OUTPUT"
          echo "major=${REDIS_VERSION%%.*}" >> "$GITHUB_OUTPUT"
          echo "variant=${REDIS_VARIANT}" >> "$GITHUB_OUTPUT"

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.DOCKER_IMAGE }}
          tags: |
            type=raw,value=${{ steps.redis_version.outputs.tag }},enable={{is_default_branch}}
            type=raw,value=${{ steps.redis_version.outputs.minor }}-${{ steps.redis_version.outputs.variant }},enable={{is_default_branch}}
            type=raw,value=${{ steps.redis_version.outputs.major }}-${{ steps.redis_version.outputs.variant }},enable={{is_default_branch}}
            type=raw,value=${{ steps.redis_version.outputs.variant }},enable={{is_default_branch}}
            type=raw,value=latest,enable={{is_default_branch}}
      
      - name: Build and push Docker image
        uses: docker/build-push-action@v7
        with:
          context: .
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          platforms: linux/amd64,linux/arm64
          cache-from: type=gha
          cache-to: type=gha,mode=max
